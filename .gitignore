# Ignore Terraform state files (contain sensitive info)
terraform.tfstate
terraform.tfstate.backup

# Ignore the .terraform directory (stores modules and plugins)
.terraform/

# Ignore crash log files (if Terraform crashes)
crash.log

# Ignore override files (custom override files)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore Terraform plan output files
*.tfplan
*.tfplan.json

# Ignore Terraform lock file (to lock provider versions locally)
.terraform.lock.hcl

# Ignore sensitive files like backend config (don't push to git)
*.tfvars
